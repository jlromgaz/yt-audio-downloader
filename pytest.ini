[pytest]
# Register the `integration` marker so real-network e2e tests can be
# excluded from the default unit test run via: pytest -m 'not integration'
markers =
    integration: real-network e2e tests
